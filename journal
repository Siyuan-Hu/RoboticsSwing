9.28
从初始点开始晃动时，x,y变化过小，很难记录到q数组中，使用mathf.roundToInt进行四舍五入解决小数点后直接被截断的问题
9.29
当q数组为零时，选择该动作，考虑模拟退火。 gaptime过小，两个state没有区别，导致动作reward没有区别，考虑增加gaptime或者间隔状态
12.25
body：q数组包含四维：x range，速度方向（x正方向或负方向），body角度（去掉该纬度，永远为0），决策（向左或向右）
该q数组经过6～12个小时的训练能够成功训练机器人上身进行大幅度swing，但训练成型的q数组不适用于机器人从0开始进行swing。换言之，
每阶段的q数组只适用于当前能量状态。高能量状态的q数组不适用于低能量状态的q数组
尝试解决方案，为q数组增加能量纬度：动能或动能加重力势能。由于x range一定程度上已经反应了重力势能状态，为保持状态之间的独立同分布，仅尝试
增加动能状态，新q数组包含：x range，速度方向（x正方向或负方向），动能，决策（向左或向右）
12.27
状态缩小为一维（速度方向），测试能量是否守恒
1.1
使用偏离角度作为奖励函数，奖励函数为theta=arcsin（dx／l），速度方向与x偏离方向一致时奖励函数为theta，不一致时为-theta。
1.3
修改腿部增强学习，使用与上半身一致的模式，robot能够在短时间内学会swing，但不足以保持能量以及持续提升能量。
考虑修改能量变化为单位时间能量变化。